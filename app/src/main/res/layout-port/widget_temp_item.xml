<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_temp_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp">

    <!--
         See http://stackoverflow.com/questions/16278159/why-linearlayouts-margin-is-being-ignored-if-used-as-listview-row-view
		 for the extra layout wrapper.
    -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@drawable/rectangle"
        android:showDividers="middle">

        <ImageView
            android:id="@+id/widget_temp_item_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:maxWidth="42dp"
            android:maxHeight="42dp" />

        <TextView
            android:id="@+id/widget_temp_item"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="?android:attr/panelBackground"
            android:textSize="18sp" />
    </LinearLayout>

</FrameLayout>